# 使用官方 Node.js 运行时作为父镜像
FROM node:14
 
# 设置工作目录
WORKDIR /app
 
# 将 package.json 和 package-lock.json 复制到工作目录
COPY package*.json ./
 
# 安装项目依赖
RUN npm install
 
# 将项目文件复制到工作目录
COPY . .
 
# 构建 React 应用（可选，如果你需要打包的话）


RUN npm run build
 
# 暴露端口（通常是3000）
EXPOSE 3000

# 运行应用
CMD ["npm", "start"]

